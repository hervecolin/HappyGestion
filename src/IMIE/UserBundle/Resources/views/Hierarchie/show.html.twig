{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hierarchie</h1>

    <table class="record_properties fer w60">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ entity.role }}</td>
            </tr>
            <tr>
                <th>Hierarchie</th>
                <td>{{ entity.hierarchie }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('hierarchie') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a href="{{ path('hierarchie_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
